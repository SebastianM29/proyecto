<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="./css/profile-styles.css">
    <title>Document</title>
</head>
<body>
    <div class="fondo-profile">
        <div class="profile">
           {{#if info}}
        
           <img id="picture" src="{{info.perfilPicture}}" alt="imagen">
       
           <p class="profile-info" ><strong>Nombre:</strong> {{info.first_name}}</p>      
           <p class="profile-info"><strong>apellido:</strong> {{info.last_name}}</p>      
           <p class="profile-info"><strong>edad:</strong> {{info.age}}</p>      
           <p class="profile-info"><strong>email:</strong> {{info.email}}</p>  
           <form id="perfilPicture" enctype="multipart/form-data">
           <label class="data" data-id="{{info.id}}" for="upload">Foto de perfil</label>
           <input type="file" class="form-control" id="upload" placeholder="subir">
           <button class="form-control mt-2" id="fotoPerfil" type="submit">subir foto de perfil</button>
           </form>    
           {{/if}}
           <a id="boton-profile" href="/">volver a productos</a></div>
    </div>
          <script src="./js/perfil.js"></script>
  
</body>
</html>